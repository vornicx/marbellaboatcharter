const links=[...document.querySelectorAll('.sidebar nav a[href^="#"]')];const sections=links.map(link=>document.querySelector(link.getAttribute('href'))).filter(Boolean);const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(!entry.isIntersecting)continue;links.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${entry.target.id}`));}},{rootMargin:'-25% 0px -65% 0px',threshold:0});sections.forEach(section=>observer.observe(section));document.querySelector('[data-command]')?.addEventListener('click',()=>{const target=prompt('Jump to: bookings, leads, quotes, fleet, owners, maintenance, content or reports');if(!target)return;document.getElementById(target.trim().toLowerCase())?.scrollIntoView({behavior:'smooth'});});